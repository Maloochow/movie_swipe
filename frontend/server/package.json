{
  "name": "server",
  "version": "1.0.0",
  "description": "sacket.io server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently 'npm run react' 'npm run server'",
    "react": "react-scripts start",
    "server": "nodemon index.js"
  },
  "author": "Luyao Ma",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "express": "^4.17.1", 
    "nodemon": "^2.0.4",
    "socketio": "^1.0.0"
  }
}
